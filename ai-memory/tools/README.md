# Memory Tools

同じ機能をOSごとの標準的なスクリプト形式で提供する。

- Linux/macOS: `sh tools/Update-MemoryIndex.sh` / `sh tools/Test-Memory.sh`
- Windows: `pwsh -File tools/Update-MemoryIndex.ps1` / `pwsh -File tools/Test-Memory.ps1`

更新スクリプトはローカルの `MEMORY-INDEX.md` を生成する。索引ブロックは手動編集しない。
